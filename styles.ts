import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#FFF',
      alignItems: 'center',
      justifyContent: 'center',
    },
    map: {
      flex: 1,
      width: '100%'
    },
    imageProvider: {
      width: 100,
      height: 100,
      borderRadius: 100/2
  },    
  });
  